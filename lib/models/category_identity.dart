class ShoeBrand {
  final String imageURL;
  final String text;
  final void Function()? onTap;

  ShoeBrand(this.imageURL, this.text, [this.onTap]);
}
 
class SneakerType {
  final String imageURL;
  final String text;
  final void Function()? onTap;

  SneakerType(this.imageURL, this.text, [this.onTap]);
}

class Colorway{
  final String imageURL;
  final String text;
  final void Function()? onTap;

  Colorway(this.imageURL, this.text, [this.onTap]);
}

class Collaboration {
  final String imageURL;
  final String text;
  final void Function()? onTap;

  Collaboration(this.imageURL, this.text, [this.onTap]);
}